[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"

[dev]
  command = "npm run dev"
  functions = "netlify/functions"
  framework = "vite"

[functions]
  directory = "netlify/functions"
  node_bundler = "esbuild"
